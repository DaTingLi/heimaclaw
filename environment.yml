name: heimaclaw
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - -e .
    - pytest>=7.0
    - pytest-asyncio>=0.21
    - pytest-cov>=4.0
    - ruff>=0.1
    - black>=23.0
    - mypy>=1.0

---
# 为不同 Python 版本创建环境
# 使用方法：
# conda env create -f environment.yml --name heimaclaw310
# conda env create -f environment.yml --name heimaclaw311 -s python=3.11
